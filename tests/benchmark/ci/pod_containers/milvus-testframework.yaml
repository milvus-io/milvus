apiVersion: v1
kind: Pod
metadata:
  labels:
    app: milvus
    componet: testframework
spec:
  containers:
  - name: milvus-testframework
    image: registry.milvus.io/milvus/milvus-test:v0.2
    command:
    - cat
    tty: true
