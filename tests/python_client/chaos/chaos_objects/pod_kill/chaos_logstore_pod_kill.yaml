kind: Schedule
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  name: test-logstore-pod-kill
  namespace: chaos-testing
spec:
  schedule: '*/60 * * * * *'
  startingDeadlineSeconds: 60
  concurrencyPolicy: Forbid
  historyLimit: 1
  type: PodChaos
  podChaos:
    selector:
      namespaces:
        - chaos-testing
      labelSelectors:
        app.kubernetes.io/instance: milvus-chaos-log
    mode: fixed
    value: "1"
    action: pod-kill
    gracePeriod: 0