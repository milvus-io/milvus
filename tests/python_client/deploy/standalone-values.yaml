cluster:
  enabled: false
image:
  all:
    repository: ${IMAGE_REPO:-milvusdb/milvus-dev}
    tag: ${IMAGE_TAG:-master-latest}
    pullPolicy: IfNotPresent
etcd:
  replicaCount: 1
minio:
  mode: standalone
pulsar:
  enabled: false
