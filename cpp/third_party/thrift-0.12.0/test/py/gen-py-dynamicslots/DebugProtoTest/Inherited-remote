#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.12.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:dynamic,slots
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from DebugProtoTest import Inherited
from DebugProtoTest.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  i32 identity(i32 arg)')
    print('  i32 Janky(i32 arg)')
    print('  void voidMethod()')
    print('  i32 primitiveMethod()')
    print('  CompactProtoTestStruct structMethod()')
    print('  void methodWithDefaultArgs(i32 something)')
    print('  void onewayMethod()')
    print('  bool declaredExceptionMethod(bool shouldThrow)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = Inherited.Client(protocol)
transport.open()

if cmd == 'identity':
    if len(args) != 1:
        print('identity requires 1 args')
        sys.exit(1)
    pp.pprint(client.identity(eval(args[0]),))

elif cmd == 'Janky':
    if len(args) != 1:
        print('Janky requires 1 args')
        sys.exit(1)
    pp.pprint(client.Janky(eval(args[0]),))

elif cmd == 'voidMethod':
    if len(args) != 0:
        print('voidMethod requires 0 args')
        sys.exit(1)
    pp.pprint(client.voidMethod())

elif cmd == 'primitiveMethod':
    if len(args) != 0:
        print('primitiveMethod requires 0 args')
        sys.exit(1)
    pp.pprint(client.primitiveMethod())

elif cmd == 'structMethod':
    if len(args) != 0:
        print('structMethod requires 0 args')
        sys.exit(1)
    pp.pprint(client.structMethod())

elif cmd == 'methodWithDefaultArgs':
    if len(args) != 1:
        print('methodWithDefaultArgs requires 1 args')
        sys.exit(1)
    pp.pprint(client.methodWithDefaultArgs(eval(args[0]),))

elif cmd == 'onewayMethod':
    if len(args) != 0:
        print('onewayMethod requires 0 args')
        sys.exit(1)
    pp.pprint(client.onewayMethod())

elif cmd == 'declaredExceptionMethod':
    if len(args) != 1:
        print('declaredExceptionMethod requires 1 args')
        sys.exit(1)
    pp.pprint(client.declaredExceptionMethod(eval(args[0]),))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
