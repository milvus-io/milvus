[package]
name = "thrift-test"
version = "0.1.0"
license = "Apache-2.0"
authors = ["Apache Thrift Developers <dev@thrift.apache.org>"]
publish = false

[dependencies]
clap = "<2.28.0"
env_logger = "0.4.0"
log = "0.3.7"
ordered-float = "0.3.0"
try_from = "0.2.0"

[dependencies.thrift]
path = "../../lib/rs"

